
const API_KEY = "";

export default function getAllProducts(user_id) {
    //Does a get request to the REST API -> returns a list of Product Objects
    //returns array of product objects


}


export default function postProduct(user_id, url) {
    //calls the lambda function with the url as its parameter
    //returns a product object in json
    //does a post request to the REST API -> REST API updates DynamoDb


}